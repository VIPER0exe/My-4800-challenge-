import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.raadm4121.pushupplanktracker',
  appName: 'Pushup and Plank Tracker',
  webDir: 'dist'
};

export default config;
